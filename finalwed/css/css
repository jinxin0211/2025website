/* 全站基礎背景：黑＋黑灰漸層 */
body {
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Noto Sans TC",
    "Microsoft JhengHei", sans-serif;
  background: radial-gradient(circle at top, #111827 0, #020617 55%),
    radial-gradient(circle at bottom, #111827 0, #000000 55%);
  color: #f5f5f5;
}

/* 導覽列 */
.lol-navbar {
  background: #000000;
  border-bottom: 1px solid rgba(148, 163, 184, 0.35);
}

.lol-navbar .navbar-brand {
  letter-spacing: 0.08em;
  text-transform: uppercase;
}

.lol-navbar .nav-link {
  cursor: pointer;
}

/* Hero：全螢幕＋黑灰背景 */
.hero-section {
  position: relative;
  min-height: 100vh;
  color: #f9fafb;
  overflow: hidden;
}

/* Hero 區背景圖 */
.player-hero {
  position: relative; /* 確保可以放 ::before */
  background:
    radial-gradient(circle at 20% 0%, #374151 0, transparent 45%),
    radial-gradient(circle at 80% 100%, #111827 0, transparent 45%),
    linear-gradient(to bottom, #020617, #000000);
}

/* 🔥 背景圖層（在整個 Hero 後面鋪滿） */
.player-hero::before {
  content: "";
  position: absolute;
  inset: 0;
  background:
    url("../images/bg.jpg") center 30% / cover no-repeat;
  opacity: 0.35;
  pointer-events: none;
  z-index: 0;
}


/* 讓內容在背景圖之上 */
.hero-section > .container {
  position: relative;
  z-index: 1;
}


/* Hero 文字 */
.hero-tag {
  font-size: 0.9rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: #9ca3af;
}

.hero-title {
  font-size: clamp(2.3rem, 4vw, 3.1rem);
  font-weight: 800;
  line-height: 1.1;
}

.hero-subtitle {
  max-width: 26rem;
  margin-left: auto;
  margin-right: auto;
  color: #e5e7eb;
}

/* 角色堆疊容器 */
.player-stack {
  position: relative;
  width: min(90vw, 960px);
  height: clamp(300px, 46vw, 400px);
  margin-top: 2.4rem;

  /* ⭐ 整排往左移一點 */
  transform: translateX(-100px);  /* 想再左一點就 -60px、-80px */
}



/* 🔥 單張角色 PNG 外框：大小一致，給 GSAP 用 */
.player-img {
  position: absolute;
  top: 50%;
  left: 50%;
  /* 🔼 角色整體放大一點 */
  width: clamp(150px, 16vw, 300px);
  height: clamp(250px, 32vw, 340px);
  transform: translate(-50%, -50%);
 
  overflow: visible;
}



/* PNG：腳對齊底部，中間最大、其他按 GSAP scale 縮放 */
.player-img img {
  position: absolute;
  left: 50%;
  bottom: 0;
  transform: translateX(-50%);
  height: 100%;
  width: auto;
  object-fit: contain;
  display: block;

  /* 🔥 陰影跟著人物輪廓走，而不是整個矩形 */
  filter: drop-shadow(0 22px 35px rgba(0, 0, 0, 0.95));
}


/* 滾動提示 */
.scroll-hint {
  font-size: 0.85rem;
  color: #9ca3af;
  display: inline-flex;
  align-items: center;
}

.scroll-dot {
  width: 8px;
  height: 8px;
  border-radius: 999px;
  border: 1px solid #9ca3af;
  position: relative;
  overflow: hidden;
}

.scroll-dot::after {
  content: "";
  position: absolute;
  inset: 1px;
  border-radius: inherit;
  background: #9ca3af;
  animation: scroll-dot 1.1s infinite;
}

@keyframes scroll-dot {
  0% {
    transform: translateY(-130%);
    opacity: 0;
  }
  40% {
    transform: translateY(0);
    opacity: 1;
  }
  100% {
    transform: translateY(130%);
    opacity: 0;
  }
}

/* 戰隊簡介區 */
.section-team {
  background: radial-gradient(circle at top, #020617 0, #000000 55%);
  border-top: 1px solid rgba(55, 65, 81, 0.7);
}

.section-team h2 {
  color: #e5e7eb;
}

.section-team p {
  color: #9ca3af;
}

/* 關於本站 */
.section-about {
  background: linear-gradient(to bottom, #020617, #000000);
  border-top: 1px solid rgba(55, 65, 81, 0.7);
}

.section-about h2 {
  color: #e5e7eb;
}

.section-about p {
  color: #9ca3af;
}

/* RWD 微調 */
@media (max-width: 576px) {
  .player-stack {
    height: 300px;
  }
}

/* 🔊 BGM 面板 */
.music-panel {
  position: fixed;
  right: 1.5rem;
  bottom: 1.5rem;
  z-index: 1000;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.55rem 0.9rem;
  border-radius: 999px;
  border: 1px solid rgba(148, 163, 184, 0.7);
  background: radial-gradient(circle at top, #111827, #020617);
  color: #e5e7eb;
  box-shadow: 0 10px 25px rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(10px);
}

/* 播放/暫停按鈕 */
.music-toggle {
  border: none;
  background: transparent;
  color: inherit;
  font-size: 0.85rem;
  cursor: pointer;
  padding: 0;
}

.music-toggle.on {
  color: #22c55e;
}

/* 曲名 + 下拉選單 */
.music-info {
  display: flex;
  flex-direction: column;
  gap: 0.15rem;
  min-width: 140px;
}

.music-title {
  font-size: 0.8rem;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.music-select {
  font-size: 0.75rem;
  background: rgba(15, 23, 42, 0.9);
  color: #e5e7eb;
  border-radius: 999px;
  border: 1px solid rgba(148, 163, 184, 0.8);
  padding: 0.12rem 0.5rem;
}

/* 小螢幕時 panel 縮小一點 */
@media (max-width: 576px) {
  .music-panel {
    right: 0.75rem;
    bottom: 0.75rem;
    padding: 0.45rem 0.7rem;
  }

  .music-info {
    min-width: 120px;
  }
}

/* 🔥 GSAP 橫向滑動畫面區 */
.gsap-gallery {
  position: relative;
  height: 100vh;              /* 區塊全螢幕高 */
  overflow: hidden;
  background: #050816;        /* 可以改成你喜歡的深色 */
  color: #f9fafb;
}

/* 裡面的軌道：橫向排列 */
.gsap-track {
  position: absolute;
  inset: 0;
  display: flex;
}

/* 每一個橫向 Panel（寬度 = 一整個畫面） */
.gsap-panel {
  min-width: 100vw;           /* 每個 panel 佔一整個視窗寬 */
  height: 100vh;
  padding: 5rem 10vw;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  justify-content: center;
  gap: 1.5rem;
}

/* 你可以針對每一頁改背景色，模仿 GSAP 那種多彩區塊 */
.gsap-panel.panel-1 {
  background: radial-gradient(circle at top, #111827, #020617);
}

.gsap-panel.panel-2 {
  background: radial-gradient(circle at top, #1d3557, #020617);
}

.gsap-panel.panel-3 {
  background: radial-gradient(circle at top, #4c1d95, #020617);
}

.gsap-panel.panel-4 {
  background: radial-gradient(circle at top, #14532d, #020617);
}

/* === 橫向滑動區：大圖 + 文字 更美觀版本 === */

.gsap-panel {
  min-width: 100vw;
  height: 100vh;
  padding: 6rem 8vw;
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
}

/* ⭐主要內容：圖片大＋文字靠側邊 */
.panel-content {
  display: flex;
  align-items: center;
  gap: 5rem; /* 讓畫面更寬敞 */
  width: 100%;
  max-width: 1400px; /* 類似官方網站寬度 */
  margin: 0 auto;
}

/* 左側圖片：更大、更有氣勢 */
.panel-media {
  flex: 0 0 55%; /* 佔整個畫面 55% 寬度（變大） */
}

/* .panel-media img {
  width: 100%;
  height: auto;
  display: block;

  border-radius: 1.2rem;
  box-shadow: 0 35px 60px rgba(0, 0, 0, 0.65);
  object-fit: cover;

  /* LoL 頁面常用：輕微的邊緣光 */
  /* filter: drop-shadow(0 20px 40px rgba(230, 48, 48, 0.7));
} */ */

/* 右側文字 */
.panel-text {
  flex: 1;
  color: #e5e7eb;
}

.panel-text h2 {
  font-size: clamp(2.6rem, 4vw, 3.8rem);
  font-weight: 800;
  margin-bottom: 1.2rem;

  background: linear-gradient(to right, #ffffff, #cbd5e1);
  -webkit-background-clip: text;
  color: transparent;
}

.panel-text p {
  font-size: 1.15rem;
  line-height: 1.75;
  color: #cbd5e1;
  margin-bottom: 1rem;
}

.panel-text ul {
  margin-top: 1rem;
  padding-left: 1.2rem;
  color: #94a3b8;
  font-size: 1rem;
  line-height: 1.6;
}


/* === RWD：手機版改為上下堆疊，圖片佔比縮小 === */
/* 左側圖片：縮小一點比例 */
.panel-media {
  flex: 0 0 40%;      /* 原本是 55%，改成 40% 就不會那麼大 */
  max-width: 480px;   /* 再加一個最大寬度限制 */
}

/* 圖片本身加上最大高度 */
.panel-media img {
  width: 100%;
  height: auto;        /* 🔥 保持原比例，不裁切 */
  max-height: 70vh;    /* ❗ 控制整體高度，但不裁切頭 */
  object-fit: contain; /* 🔥 contain = 完整顯示整張圖片 */
  display: block;

  border-radius: 1.2rem;
  box-shadow: 0 35px 60px rgba(0, 0, 0, 0.65);
  filter: drop-shadow(0 20px 40px rgba(0, 0, 0, 0.7));
}



.fmvp-year-tag {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
  padding: 0.2rem 0.75rem;
  border-radius: 800px;
  border: 1px solid rgba(148, 163, 184, 0.7);
  font-size: 0.8rem;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: #e5e7eb;
  margin-bottom: 0.75rem;
  background: radial-gradient(circle at top, rgba(15,23,42,0.9), rgba(15,23,42,0.4));
}

/* 🔥 左上角 FMVP 浮動標題 */
.fmvp-badge {
  position: absolute;
  top: 5rem;
  left: 6vw;
  z-index: 10;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 0.2rem;
  pointer-events: none;          /* 不要擋到滑鼠 */
}

.fmvp-badge-main {
  font-size: 1.1rem;
  letter-spacing: 0.28em;
  text-transform: uppercase;
  padding: 0.35rem 0.9rem;
  border-radius: 999px;
  border: 1px solid rgba(15, 103, 226, 0.7);
  background: radial-gradient(circle at top, rgba(15,23,42,0.95), rgba(205, 206, 210, 0.6));
  color: #e5e7eb;
}

.fmvp-badge-sub {
  font-size: 0.8rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: #9ca3af;
  margin-left: 0.2rem;
}

/* 手機版微調 */
@media (max-width: 768px) {
  .fmvp-badge {
    top: 4rem;
    left: 5vw;
  }

  .fmvp-badge-main {
    font-size: 0.9rem;
    letter-spacing: 0.22em;
  }

  .fmvp-badge-sub {
    font-size: 0.7rem;
  }
}

/* 🔥 下方文字列表 */
.fmvp-info-list {
  margin: 0;
  padding-left: 1.2rem;
  color: #cbd5e1;
  font-size: 1rem;
  line-height: 1.7;
}

/* 代表英雄區塊 */
.fmvp-heroes {
  margin-top: 1.6rem;
  display: flex;
  align-items: center;
  gap: 1.2rem;
  flex-wrap: wrap;
}

.fmvp-heroes-label {
  font-size: 0.9rem;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: #94a3b8;
}

.fmvp-hero-list {
  display: flex;
  gap: 1.2rem;
  flex-wrap: wrap;
}

/* 🔥 每顆 200px 大卡片，在 flex 裡不會互相擠壓 */
.hero-icon {
  flex: 0 0 200px;   /* 固定寬 200px */
  width: 200px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0.4rem;
}

/* 圖片 200 x 200，套在卡片裡 */
.hero-icon img {
  width: 100%;       /* ❗ 不要再用 200% 了，會溢出去 */
  height: 200px;
  border-radius: 18px;
  object-fit: cover;
  box-shadow: 0 14px 26px rgba(0, 0, 0, 0.75);
  border: 1px solid rgba(148, 163, 184, 0.9);
  background: radial-gradient(circle at top, #020617, #111827);
}

.hero-icon span {
  font-size: 0.85rem;
  color: #e5e7eb;
}

/* 📱 小螢幕縮小一點 */
@media (max-width: 576px) {
  .hero-icon {
    flex: 0 0 140px;
    width: 140px;
  }

  .hero-icon img {
    height: 140px;
  }

  .hero-icon span {
    font-size: 0.75rem;
  }
}

/* 左側圖片容器一樣就好 */
.panel-media1 {
  flex: 0 0 40%;
  max-width: 480px;
}

/* 固定一個比例的框：這邊用 3:4，你可以自己改 */
.fmvp-photo {
  width: 100%;
  aspect-ratio: 3 / 4;           /* 控制長寬比例 → 所有人都同一個比例 */
  border-radius: 24px;
  box-shadow: 0 35px 60px rgba(0, 0, 0, 0.65);
  background-size: cover;        /* 塞滿框 */
  background-position: center;   /* 以中間為主 */
  background-repeat: no-repeat;
  filter: drop-shadow(0 20px 40px rgba(0, 0, 0, 0.7));
}

/* 各年份指定不同圖片 */
.fmvp-photo.fmvp-2022 {
  background-image: url("../images/kingen.png");
}

.fmvp-photo.fmvp-2023 {
  background-image: url("../images/zeus.png");
}

.fmvp-photo.fmvp-2025 {
  background-image: url("../images/guma\ png.png");
}
.fmvp-photo.fmvp-2024 {
  background-image: url("../images/9132.png");
}



/* 之後 2024、2025 要加直接新增 class 就好 */
